// Even or Odd
// Create a function evenOrOdd 
// that takes a number parameter and returns 'even' if the number is even, or 'odd' if the number is odd.

function evenOrOdd(number) {
    if(number % 2 === 0) {
        return 'even'
    }
    return 'odd'
}